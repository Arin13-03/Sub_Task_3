name: Creative Workflow
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Display a creative message
        run: echo "Hello, this is a creative workflow!"
